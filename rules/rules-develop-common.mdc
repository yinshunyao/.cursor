---
description: "workspace 通用开发纪律：设计先行、编码前核对 DR、变更顺序（适用于维护标准 doc 树的子项目，与各工程 language-specific rules-develop 配合）"
globs:
  - "**/*"
alwaysApply: true
---

# 通用开发编码原则（workspace）

> 本规则适用于本 workspace 下在 `{工程}/doc/` 中维护「原始需求、开发设计、测试设计」标准目录（`01-or` / `02-dr` / `03-tr` 等）的**工程子项目**。文中 `{工程}` 表示子项目根目录名（如 `codiiy`、`ls`），路径均相对于 workspace 根目录。
>
> **与端到端流程的关系**：需求评审 → 设计 → 测试设计 → 实现 → 归档的完整顺序以 `.cursor/rules/rules-workflow.mdc` 为准。本节只约束**进入编码阶段**时的纪律。
>
> **与工程内规则的关系**：语言栈、依赖文件、框架特有约定（如某子项目的 Python / Django 细则）以该工程下的 `rules-develop`（若存在）为准；本文件不替代子项目级编码规范。

## 设计先行原则（强制）

1. **编码前检查设计文档**：开始编写或修改功能代码之前，**必须**先确认 `{工程}/doc/02-dr/` 中存在覆盖本次变更的设计文档。若不存在或未覆盖，需先补充设计文档。
2. **设计文档优先于代码**：当发现问题时，先检查设计文档是否存在问题，先修改设计文档，再修改代码实现。
3. **变更顺序**：需求变更 → 更新 OR 文档(`{工程}/doc/01-or/`) → 更新设计文档(`{工程}/doc/02-dr/`) → 更新测试设计(`{工程}/doc/03-tr/`) → 修改代码 → 修改测试代码。严禁反向操作。

## Python：`if __name__ == "__main__"` 与命令行入口（约定）

适用于子项目以 Python 实现的脚本或可执行模块。

1. **范例 / 本地试跑**：在 `if __name__ == "__main__":` 中编写的**调试、示例或一次性试跑**代码（常见为改几行变量后直接在 IDE 中运行），**采用模块内变量参数**（如在 `__main__` 块或紧邻位置定义 `INPUT_PATH`、`FLAGS` 等），**不要**为此类场景单独依赖 `argparse` / `sys.argv`。
2. **正式从命令行调用**：当脚本的**主要使用方式**是从 shell / 调度系统传入参数时，**应**提供命令行参数解析（如 `argparse`），并在文档或 `--help` 中说明用法。
3. **生产运行**：部署与自动化流水线中，**优先**通过命令行（或等价环境）传递参数；与第 1 条不矛盾：`__main__` 里仍可保留一组默认变量作为开发时的快捷入口，解析逻辑与正式 CLI 入口可共用同一套函数/配置对象，避免两套不一致的入口。

## 执行时注意

- 若子项目的 `rules-develop`（如 `codiiy` 下的版本）与本文件对「设计先行、变更顺序」的表述一致，二者应同时遵守；若有冲突，以**子项目已声明的更高优先级约束**及**原始需求 `01-or`** 为准。
