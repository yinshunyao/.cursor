---
description: "全局强制规则：定义从用户对话到需求、设计、开发、测试、质量保障的端到端工作流程"
alwaysApply: true
---

# 端到端开发工作流规范（workspace）

> 本规则适用于本 workspace 下在 `{工程}/doc/` 中维护「原始需求、开发设计、测试设计、质量保障、运维发布」标准目录树（`01-or` / `02-dr` / `03-tr` / `91-qa` / `99-ops`）的**工程子项目**。文中 `{工程}` 表示子项目根目录名（如 `codiiy`、`ls`），路径均相对于 workspace 根目录。

对已声明为**外部只读引用**的目录（如 `paperclip/`、`agency-agents-zh/`），不得在其内进行需求驱动的交付流程；研读与引用见对应只读规则。

本规则定义从用户对话到交付的完整执行顺序。**在适用范围内的开发活动都必须按照本流程执行，不允许跳过环节。**

### 与工程内细化规则的关系

- 全局文档结构与章节标准：`.cursor/rules/rules-or.mdc`、`.cursor/rules/rules-dr.mdc`。
- 全局测试设计、91-qa 章节约束与自动化测试要求：`.cursor/rules/rules-test-design.mdc`。
- 全局测试代码目录映射与 E2E（Playwright）组织：`.cursor/rules/rules-test.mdc`。
- 编码实现规范：若存在 `.cursor/rules/{工程}/rules-develop.mdc`，则须遵循对应工程内规则。

## 场景一：用户提出新需求或功能变更

按以下顺序执行，每一步完成后再进入下一步：

```
① 整理需求(01-or) → ② 开发设计(02-dr) + 测试设计(03-tr) → ③ 代码实现 + 自动化测试 → ④ 归档工作项
```

### ① 整理/更新原始需求
- 检查 `{工程}/doc/01-or/` 中是否已有该主题的 OR 文档。
- 有 → 将新内容融入对应章节（背景/目标/要求），遵循 `.cursor/rules/rules-or.mdc`。
- 无 → 新建 OR 文档，遵循 `.cursor/rules/rules-or.mdc` 的命名和章节结构。
- 在 `# 当前工作项` 中登记本次任务。

### ② 同步完成开发设计和测试设计
- 在 `{工程}/doc/02-dr/` 中创建或更新设计文档，遵循 `.cursor/rules/rules-dr.mdc`，必须标注对应 OR 文档路径。
- **同时**在 `{工程}/doc/03-tr/` 中创建或更新测试设计文档；细则遵循 `.cursor/rules/rules-test-design.mdc`。
- 开发设计和测试设计是**同一步骤的两个产出**，不允许先完成一个再补另一个。

### ③ 代码实现与自动化测试
- 编写功能代码；编码规范遵循该工程的 `rules-develop.mdc`（若已提供）。
- **同步**编写自动化测试代码：目录映射、E2E 约定等遵循 `.cursor/rules/rules-test.mdc`。
- 实现前**必须**先确认设计文档已覆盖本次变更。
- 若本次实现影响部署命令、启动参数、环境变量或运行目录约束，必须同步更新 `{工程}/doc/99-ops/`（至少 `run.md`，具体以该工程 `doc/99-ops/AGENTS.md` 为准）。

### ④ 归档工作项
- 将 OR 文档中 `# 当前工作项` 的已完成条目迁移到 `# 已完成工作项`。
- 确认设计文档和测试设计文档与最终实现一致。
- 发布记录采用「草稿先行」策略：细小功能/变更/问题修复先记录到 `{工程}/doc/99-ops/release-note-draft.md`，不强制实时改 `{工程}/doc/99-ops/release-note.md`。
- `{工程}/doc/99-ops/release-note.md` 仅在收到「发布整理」或「发布汇总」指令后，从草稿中提炼并精简更新。

## 场景二：用户反馈 Bug 或问题

按以下顺序执行：

```
① 创建QA文档(91-qa) → ② 分析定位 → ③ 修复代码 → ④ 回填文档 + 补充回归测试
```

### ① 创建 QA 文档
- 在 `{工程}/doc/91-qa/` 中创建问题文档；章节约束遵循 `.cursor/rules/rules-test-design.mdc` 中关于 91-qa 的说明。
- 先写 `# 问题描述`，记录现象和复现条件。
- 填写 `# 关联文档`，定位该问题**所属的原始需求（01-or）和开发设计（02-dr）文档**，建立追溯关系。若暂时无法确定，标注「待定位」并在后续步骤中回填。

### ② 分析定位根因
- 排查问题根本原因，在 QA 文档中填写 `# 根因分析`。
- 根据分析结果，**确认并完善** `# 关联文档` 中的文档路径（补充关联的测试设计文档 03-tr 路径）。

### ③ 修复代码
- 若修复涉及设计变更，**先更新** `{工程}/doc/02-dr/` 中的设计文档，再修改代码。
- 若 Bug 暴露出需求遗漏，需同步在 `{工程}/doc/01-or/` 中补充需求。

### ④ 回填文档与补充测试
- 在 QA 文档中补全 `# 涉及文件` 和 `# 设计约束更新`（如有）。
- **最终确认** `# 关联文档` 章节完整，包含所有关联的 01-or、02-dr、03-tr 文档路径。
- 在 `{工程}/doc/03-tr/` 中补充回归测试用例。
- 编写对应的自动化回归测试代码。

## 场景三：仅修改设计文档（无代码变更）

- 修改 `{工程}/doc/02-dr/` 时，必须同步检查并更新 `{工程}/doc/03-tr/` 中对应的测试设计。
- 修改 `{工程}/doc/01-or/` 时，必须检查下游 `{工程}/doc/02-dr/` 和 `{工程}/doc/03-tr/` 是否需同步更新。
- 若设计变更包含运维部署影响（启动方式、环境变量、命令参数、目录基准），必须同步检查并更新 `{工程}/doc/99-ops/`。

## 禁止行为

1. **禁止跳过需求直接写代码**：无论多小的功能变更，都必须先确认 OR 文档中有记录。
2. **禁止跳过设计直接写代码**：代码实现前必须有对应的设计文档覆盖。
3. **禁止设计不同步**：修改开发设计而不检查测试设计。
4. **禁止 Bug 修复不留痕**：用户反馈的问题必须在 91-qa 中有文档记录。
5. **禁止代码无测试**：功能代码提交时必须有对应的自动化测试。
6. **禁止 QA 文档无追溯**：91-qa 中的每个问题文档必须通过 `# 关联文档` 章节关联到对应的原始需求(01-or)和设计文档(02-dr/03-tr)，确保问题可反向追溯到需求和设计。
7. **禁止部署变更不更新运维文档**：涉及启动/部署约束变更时，不得遗漏 `{工程}/doc/99-ops/` 同步。
8. **禁止发布记录散落**：细小发布项不得分散在聊天记录或临时文件，必须统一沉淀到 `{工程}/doc/99-ops/release-note-draft.md`。

## 文档目录速查

| 目录 | 用途 | 对应规则（全局 / 工程内） |
| :--- | :--- | :--- |
| `{工程}/doc/01-or/` | 原始需求 | `.cursor/rules/rules-or.mdc` |
| `{工程}/doc/02-dr/` | 开发设计 | `.cursor/rules/rules-dr.mdc` |
| `{工程}/doc/03-tr/` | 测试设计 | `.cursor/rules/rules-test-design.mdc` |
| `{工程}/doc/91-qa/` | 质量保障 | `.cursor/rules/rules-test-design.mdc` |
| `{工程}/doc/99-ops/` | 运维发布 | `{工程}/doc/99-ops/AGENTS.md` |
| `{工程}/test/` | 单元/集成测试代码 | `.cursor/rules/rules-test.mdc` |
| `{工程}/test/e2e/` | E2E 测试代码 | `.cursor/rules/rules-test.mdc` |
