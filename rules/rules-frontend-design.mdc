---
description: "前端界面与样式：高完成度、有辨识度的界面实现；避免通用「AI 审美」；适用于页面/组件/HTML/CSS 等前端实现与美化"
globs:
  - "**/*.vue"
  - "**/*.tsx"
  - "**/*.jsx"
  - "**/*.css"
  - "**/*.scss"
  - "**/*.less"
  - "**/*.html"
  - "**/*.svelte"
alwaysApply: false
---

# 前端界面设计（Frontend Design）

> 思路源自 Anthropic Claude `frontend-design` 技能，经本项目化改写为 Cursor 规则。  
> 当用户要求搭建 Web 组件、页面、落地页、仪表盘、或对界面做样式与美化时，在**不与子工程既有设计系统、组件库约定冲突**的前提下，遵循本节。

目标是产出**可运行、可上线**的前端代码，并在视觉与交互上有明确风格与记忆点，避免千篇一律的「AI 模板感」。

## 适用与优先级

- **适用**：组件、页面、整站、海报式落地页、仪表盘、React/Vue/HTML 布局、样式与动效。
- **冲突时**：子项目内已有设计令牌、组件规范、`.cursor/rules` 中更细的前端规则优先；本规则补充**审美方向与实现质量**，不覆盖业务或框架强制约定。

## 设计思维（编码前）

先理解场景，再**选定一条清晰、偏 bold 的美学方向**：

| 维度 | 说明 |
|:---|:---|
| **目的** | 界面解决什么问题？主要使用者是谁？ |
| **气质** | 取一个明确极端或强风格：极简、极繁、复古未来、自然有机、轻奢、玩具感、杂志编辑风、粗野主义、装饰艺术、柔和粉彩、工业实用等；可混合但要有主调。 |
| **约束** | 框架、性能、可访问性（a11y）、品牌规范。 |
| **差异化** | 用户会记住的**一个**核心视觉或交互记忆点是什么？ |

**要点**：方向要清晰、执行要一致。极繁与极简都可以，关键是**有意为之**，而非堆叠或空洞。

实现时交付：

- 可运行、结构合理的生产级代码；
- 视觉有辨识度、整体统一；
- 细节（间距、层次、动效节奏）经得起推敲。

## 前端美学准则

### 字体（Typography）

- 优先选择**有特点、可读性好**的字体；避免默认万能字体堆（如 Arial、Inter、Roboto、纯系统字体栈）。
- 常见搭配：**一种有个性的大标题/展示字体 + 一种克制的正文字体**。

### 色彩与主题（Color & Theme）

- 用 **CSS 变量**维护色板与主题，保持一套体系。
- **主色 + 少量强调色**通常优于平均、胆怯的彩虹盘。

### 动效（Motion）

- 用动画做**重点时刻**与微交互；HTML/CSS 场景优先 **CSS**；React 项目在可用时使用 Motion 等库。
- **一次精心编排的入场**（如 stagger、`animation-delay`）往往比零散小动效更有印象；可结合滚动显现、有惊喜的 hover。
- 动效复杂度要与整体风格匹配（见下「实现复杂度」）。

### 版式与空间（Spatial Composition）

- 敢于打破平均网格：**不对称、叠层、斜向动线、破格元素**；或刻意高密度信息排版。
- **大留白**与**受控的密排**都是合法方向，择一执行到底。

### 背景与细节（Backgrounds & Details）

- 少用大平涂当唯一背景；可用与气质一致的**氛围**：渐变网格、噪点、几何纹理、半透明叠层、强阴影、装饰线框、颗粒覆盖等（按需选用，服务整体风格）。

### 实现复杂度与风格一致

- **极繁**：代码与动效、层次可以更丰富。
- **极简/高级**：靠留白、字号层级、细微边框与动效精度；**克制也是一种工作量**。

## 禁止的「通用 AI 审美」

除非产品/品牌明确要求，避免默认落入：

- 泛滥字体：Inter、Roboto、Arial、Space Grotesk、纯系统 UI 字体等作为「不加思考的默认」；
- 套路配色：尤其是**白底 + 紫粉渐变条**一类 cliché；
- 居中卡片无限堆叠、无上下文的通用仪表盘骨架；
- **跨项目、跨次对话收敛到同一套字体与配色**——应根据上下文变化风格。

应针对当前场景做**有上下文的设计选择**；不同任务之间在气质、明暗、字体上应有可见差异。

## 自检（交付前快速过一遍）

- [ ] 是否有一条清晰的美学主调，且组件层级与间距支持这条主调？
- [ ] 字体与色板是否刻意选择过，而非模板默认？
- [ ] 动效是「一两处高光」还是杂乱碎动效？是否与性能/a11y 约束兼容？
- [ ] 版式是否有记忆点（不对称、叠层、大胆留白或高密度之一）？
