---
description: "codiiy 开发编码规范：Python/Django 语言与依赖、路径与启动基准、ORM 建表与原生 SQL、组件调用（通用设计先行见 rules-develop-common）"
globs:
  - "codiiy/**/*"
alwaysApply: true
---

# 开发编码规范（codiiy 子项目）

> 本规则适用于 `codiiy/` 子项目，所有路径均相对于 workspace 根目录。
>
> **通用纪律**（设计先行、变更顺序）见 workspace 根规则 `.cursor/rules/rules-develop-common.mdc`，本文件不再重复。

## 语言与依赖规范（强制）

1. **开发语言**：本项目工程开发**必须**使用 Python 语言，禁止引入其他后端语言（如 Java、Go、Rust 等）实现业务逻辑。
2. **依赖管理**：当引入新的第三方依赖包时，**必须**同步更新项目根目录下的 `codiiy/requirements.txt`，记录新增的依赖及其版本约束。未更新依赖文档的代码变更不得提交。

## 路径基准规范（强制）

1. **系统根目录固定**：Django 运行时的系统根目录固定为 `codiiy/core` 的上一级目录（即 `codiiy/`）。
2. **相对路径解析基准固定**：所有业务相对路径（如 `codiiy/doc/...`、`codiiy/tools/...`）必须相对于系统根目录解析，禁止依赖当前进程工作目录（`cwd`）。
3. **目录歧义禁止**：当用户或模型输入 `doc/...` 时，统一解释为 `codiiy/doc/...`，不得解析为 `codiiy/core/doc/...`。
4. **启动一致性**：`manage.py`、`wsgi.py`、`asgi.py` 必须保持一致的根目录初始化行为，避免不同启动方式出现路径分叉。
5. **启动可配置**：Django 启动前允许显式设置项目目录（推荐参数或环境变量）；若未提供，默认使用 `core` 的上一级目录。
6. **部署文档同步**：当启动参数、环境变量、部署命令或运行目录约束发生变更时，必须同步更新 `codiiy/readme.md`（运行说明）；发布类说明仍按 `codiiy/doc/99-ops/` 约定维护。
7. **发布文档策略**：发布主文档 `codiiy/doc/99-ops/release-note.md` 不要求每次实时更新；细小新功能/变更/问题修复先记录到 `codiiy/doc/99-ops/release-note-draft.md`，待发布指令后再统一精简汇总。

## 后端数据库规范

### 核心原则

1. **表结构定义**：必须使用 ORM (如 SQLAlchemy, TypeORM, Hibernate 等) 来定义数据库表格结构和关系。
2. **数据操作**：在进行增删改查 (CRUD) 操作时，**严禁**使用 ORM 自带的查询方法，必须编写并执行原生的 SQL 语句。

### 示例

- ✅ 正确：使用 ORM 定义 Model，但在 Service 层使用 `db.execute("SELECT * FROM ...")`。
- ❌ 错误：使用 `Model.objects.filter()` 或 `repository.find()` 等 ORM 查询方法。

## 组件查询/调用规范（强制）

1. 所有非组件模块（如 `tools`、`control`、`core` 等）在查询组件信息或调用组件函数时，必须通过 `codiiy/tools/component_call_tool`。
2. 严禁在非组件模块中直接导入或直接调用 `component` 代码（包括 `component.call_by_path` 等）。
3. 若现有 `codiiy/tools/component_call_tool` 无法满足需求，应先扩展该工具，再由业务模块接入；禁止在业务模块绕过工具直连组件。

### 示例

- ✅ 正确：`tool = ComponentCallTool(); tool.control_call(function_path=..., kwargs=...)`
- ❌ 错误：`from component import call_by_path` 后在 `tools/control/core` 中直接调用
