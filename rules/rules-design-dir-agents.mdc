---
description: "workspace 结构设计/规划目录必须声明 AGENTS.md 并维护目录说明与编辑规范（全局）"
globs:
  - "**/*"
alwaysApply: true
---

# 结构设计规划目录 AGENTS 规范（全局）

> 本规则适用于本 workspace 下**任意工程目录**中承载「整体结构设计、方案设计、规划说明、测试规划、运维规划」的文档目录。文中 `{工程}` 表示子项目根目录名（如 `codiiy`、`ls`），路径均相对于 workspace 根目录。

当创建、调整或维护此类目录时，必须同时维护该目录下的 `AGENTS.md`。

## 适用范围
- 任何承载上述规划类内容的目录，以及与之职责对齐的分层子目录。
- 包含但不限于 `{工程}/doc/` 下的分层目录（如 `01-or`、`02-dr`、`03-tr`、`99-ops`）及后续新增同类目录。
- 若某子项目采用与 `{工程}/doc/` 同级或不同的规划根路径，只要属于「结构化规划文档目录树」，同样适用本规则（在 AGENTS.md 中写清该根路径即可）。

## 强制要求
1. 目录内必须存在 `AGENTS.md`，且与目录内容保持同步。
2. 新增此类目录时，首个提交必须包含 `AGENTS.md`（不得后补）。
3. 修改目录职责、文档分类、命名规则后，必须先更新 `AGENTS.md` 再编辑其他文档。
4. 需求分析类内容整合到 `{工程}/doc/01-or/`（或该工程在 AGENTS.md 中声明的等价路径）中的相关文档。
5. 开发设计类内容整合到 `{工程}/doc/02-dr/`（或等价路径）中的相关文档。
6. 测试需求与设计整合到 `{工程}/doc/03-tr/`（或等价路径）中的相关文档。
7. 运维与发布相关内容整合到 `{工程}/doc/99-ops/`（或等价路径）中的相关文档。

## AGENTS.md 最少内容
- **目录用途**：该目录解决什么问题、服务哪个阶段。
- **文档范围**：允许放入的文档类型与不应放入的内容。
- **编辑规则**：文档命名、章节约束、变更流程、完成项归档方式。
- **协作约定**：与相邻目录的边界、引用关系、优先级说明。

## 执行约束
- 若发现目录缺失 `AGENTS.md`，应先补齐再继续该目录下的内容编辑。
- 若 `AGENTS.md` 与实际结构不一致，应先修正文档规则再改实现内容。

## 与各工程规则的关系
- 端到端工作流以 `.cursor/rules/rules-workflow.mdc` 为准；全局测试代码与测试设计、91-qa 细则见 `.cursor/rules/rules-test.mdc`、`.cursor/rules/rules-test-design.mdc`；子项目内若有更细的工程规则（如 `.cursor/rules/{工程}/rules-develop.mdc`），**不得**与上述全局工作流及本目录规范冲突。流程类规则可引用本规则作为目录与 `AGENTS.md` 的底线要求。
